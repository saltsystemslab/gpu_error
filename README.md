
## GPU Error Lib

This is a little helper library to assist with building/debugging GPU applications. The main object exposed is `<gpu_error/log.cuh>`, which exposes a logging object that is instantiated on host and used on device.

The following functions are exposed:


